---
esp32:
  board: esp32dev

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

external_components:
 # source: github://batuakan/ESPHomeSignalK@main
- source:
    type: local
    path: ../components
  components: [ signalk ]

captive_portal:

logger:
  level: DEBUG

json:

api:

signalk:
  host: !secret signalk_host
  # port: 3000 (Optional defaults to 3000)