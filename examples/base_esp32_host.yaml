---

host:

external_components:
  - source:
      type: local
      path: ../components
      # source: github://batuakan/ESPHomeSignalK@main
    components: [signalk]

logger:
  level: DEBUG

json:

api:

signalk:
  host: !secret signalk_host  # port: 3000 (Optional defaults to 3000)
